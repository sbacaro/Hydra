.DS_Store
.build/
**/xcuserdata/
DerivedData/
Backplane/work/
dist/
*.driver
.swiftpm/
ThirdParty/

# xcodebuild -derivedDataPath build
build/
.xcodebuild/
# project backups
*.pbxproj.bak

# Release binaries — published to GitHub Releases, never committed
*.pkg
*.zip
*.dmg
*.app.zip

# Sparkle auto-update — generated appcast and (NEVER commit) the EdDSA private key
appcast.xml
*.private
sparkle_private_key*
