<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  DEPRECATED / UNUSED.

  Hydra used to run its audio engine as a separate `hydrad` LaunchAgent process,
  registered via SMAppService using this plist. The engine now runs IN-PROCESS
  inside Hydra.app (see Sources/hydrad/DaemonRuntime.swift and
  Sources/HydraApp/DaemonService.swift), so there is no LaunchAgent anymore.

  This file is referenced by nothing and is excluded from the build. It is kept
  only because the workspace can't delete it; it can be removed from the repo.
-->
<plist version="1.0">
<dict/>
</plist>
